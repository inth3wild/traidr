{
  "devDependencies": {
    "husky": "^9.1.5",
    "lint-staged": "^15.2.9"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "backend/**/*.{js,ts}": [
      "npx lint-staged --cwd /backend"
    ],
    "frontend/**/*.{js,ts,tsx,jsx}": [
      "npx lint-staged --cwd /frontend"
    ]
  }
}
